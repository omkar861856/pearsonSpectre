// For Vite, we'll use CSS imports in our main.tsx or index.css instead of Next.js font loading

// These are empty exports to maintain compatibility with code importing these constants
export const Inter = {
  variable: '--font-inter'
};

export const Merriweather = {
  variable: '--font-merriweather'
};
